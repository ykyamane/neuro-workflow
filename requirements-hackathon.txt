# Hackathon "venv + pip" path — run from the repository root inside an
# activated virtualenv:
#
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements-hackathon.txt
#
# `-e .[pointnet,nest]` installs neuroworkflow + bmtk/h5py/matplotlib (pointnet
# extra) + nest-simulator (nest extra, from its PyPI wheel).
#
# NEST ships PyPI wheels only for macOS 15+ (arm64/x86_64) and Linux x86_64
# (CPython 3.9–3.13). On other platforms (older macOS, Linux aarch64, Windows)
# use the conda path instead — see environment.yml / HACKATHON_SETUP.md.
-e .[pointnet,nest]
jupyterlab
